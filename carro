import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner entrada = new Scanner(System.in);

        System.out.println("ingrese la  velocidad limite: ");
        int S = entrada.nextInt();

        System.out.println("ingresa la distancia: ");
        double D = entrada.nextDouble();

        System.out.println("ingrese el tiempo: ");
        double T = entrada.nextDouble();

        while(S < 10 || S > 10000 ||
        D < 0 || D > 100000 ||
        T < 0 || T > 100000){

            System.out.println("Datos incorrectos");

            System.out.println("ingrese la  velocidad limite: ");
            S = entrada.nextInt();

            System.out.println("ingresa la distancia: ");
            D = entrada.nextDouble();

            System.out.println("ingrese el tiempo: ");
            T = entrada.nextDouble();
        }

        double Velocidad = S * 5280.0 / 3600.0;
        double Tiempo = D / Velocidad;

        if (Tiempo <= T){
            System.out.print("Lo logro ");
        } else {
            System.out.print("prueba fallida");
        }
    }
}